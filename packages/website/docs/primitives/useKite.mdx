---
---

import SimpleUseKite from "@site/src/components/docs/SimpleUseKite";
import PolygonProps from "@site/docs/primitives/_polygonProps.mdx";
import PrimitiveConfig from "@site/docs/primitives/_primitiveConfig.mdx";
import PrimitiveReturns from "@site/docs/primitives/_primitiveReturns.mdx";

# useKite

Hook used to a kite.

It can be either a **convex** or **concave** kite. A **convex** kite is the usual kite, which every vertices points outward.

On the other hand, a **concave** kite, the vertex of shorter sides are point inward, which will form a v-shape or upside-down dart.

<SimpleUseKite />

## Configuration

**_(KiteConfig | undefined)_** `useKite` accepts following configurations:

| Property    |   Type    | Default | Description                                               |
| :---------- | :-------: | :-----: | :-------------------------------------------------------- |
| `tLength`   | `number`  |  `50`   | Upper side length of a kite _(usually the shorter side)_. |
| `bLength`   | `number`  |  `75`   | Lower side length of a kite _(usually the longer side)_.  |
| `isConcave` | `boolean` | `false` | Is the kite concave?                                      |

<PrimitiveConfig />

:::tip

You can create a dart/arrow by creating a **concave** kite and using `rotation` to change its pointing direction.

:::

:::note

Multiple kites can be created by 2 same side lengths, either on the "slimmer" or "squarer" side.

Right now, the implementation will always give **one same** kite by the same side lengths provided.
We're thinking of adding an `angle` config to define the elevating angle of shorter side against the x-axis _(horizontal line)_, but hold it because it may not makes a great mathematical sense for creating a kite.

If you need to define that angle, please try [<code>usePolygon(\{ vertices \})</code>](/docs/primitives/usePolygon#configuration) instead, by defining the vertices yourself.

:::

## Returns

<PrimitiveReturns configType="KiteConfig" />

## Example

```tsx
import { useKite } from "react-use-polygon";

// A convex kite
const kite = useKite({ length: 50, length: 75 });

// A dart/arrow poiting right
const arrow = useKite({ isConcave: true, rotation: 90 });
```
