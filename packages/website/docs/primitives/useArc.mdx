---
---

import SimpleUseArc from "@site/src/components/docs/SimpleUseArc";
import PolygonProps from "@site/docs/primitives/_polygonProps.mdx";
import PrimitiveConfig from "@site/docs/primitives/_primitiveConfig.mdx";
import PrimitiveReturns from "@site/docs/primitives/_primitiveReturns.mdx";

# useArc

Hook used to create arc of a circle.

<SimpleUseArc />

## Configuration

:::note

`useArc` will draw an arc counter-clockwise, starting from the right center position ~~_(just like how trigo normally works)_~~.

If you wish to create an arc facing to other side, please use `rotation`.

:::

**_(ArcConfig | undefined)_** `useArc` accepts following configurations:

| Property |   Type   | Default | Description                   |
| :------- | :------: | :-----: | :---------------------------- |
| `radius` | `number` |  `100`  | Radius of the arc.            |
| `angle`  | `number` |  `60`   | Angle of the arc, in degrees. |

<PrimitiveConfig />

## Returns

<PrimitiveReturns configType="ArcConfig" />

## Example

```tsx
import { useArc } from "react-use-polygon";

const arc = useArc({ radius: 80, angle: 45, rotation: 30 });
```
